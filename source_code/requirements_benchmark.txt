# Requirements for nuclei segmentation benchmarking
# Install: pip install -r requirements_benchmark.txt

# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
scikit-image>=0.19.0
scikit-learn>=1.0.0
tqdm>=4.62.0
Pillow>=8.0.0
tifffile>=2021.0.0

# For HoVerNet
tiatoolbox>=1.4.0
torch>=1.10.0
torchvision>=0.11.0
joblib>=1.1.0

# For StarDist
stardist>=0.8.0
tensorflow>=2.6.0  # Or tensorflow-gpu for GPU support

# For CellViT (optional - may need separate installation)
# transformers>=4.20.0  # For HuggingFace backend

# For dataset download
kaggle>=1.5.0
datasets>=2.0.0  # For HuggingFace datasets

# For visualization/reports
matplotlib>=3.4.0
tabulate>=0.8.0  # For markdown table generation

# XML parsing (for MoNuSeg annotations)
# Uses stdlib xml.etree.ElementTree - no extra package needed
